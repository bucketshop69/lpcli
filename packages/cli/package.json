{
  "name": "@lpcli/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "LPCLI CLI — lpcli discover, open, close, positions...",
  "bin": {
    "lpcli": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@lpcli/core": "workspace:*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
