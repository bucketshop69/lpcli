/**
 * @lpcli/mcp — MCP Server
 *
 * Exposes all LPCLI tools via the Model Context Protocol.
 * Supports stdio (for OpenClaw, Claude Desktop) and
 * Streamable HTTP (for remote MCP clients).
 *
 * TODO: Implement after @lpcli/core works:
 * - Tool definitions with Zod schemas
 * - McpServer setup with StdioServerTransport
 * - All 10 tools wired to @lpcli/core
 */

export {};
