{
  "name": "@lpcli/mcp",
  "version": "0.1.0",
  "type": "module",
  "description": "LPCLI MCP server — exposes LPCLI tools via Model Context Protocol",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@lpcli/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
